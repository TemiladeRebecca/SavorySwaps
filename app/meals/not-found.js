export default function NotFound() {
    return <main className="not-found">
        <h1>Meal not found</h1>
        <p>Sorry we could not display the requested page or meal detail...</p>
    </main>
}